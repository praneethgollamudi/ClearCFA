name: Deploy Supabase

on:
  push:
    branches: [main]
    paths:
      - 'supabase/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run SQL migrations
        env:
          PAT: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          for f in $(ls supabase/migrations/*.sql 2>/dev/null | sort); do
            echo "▶ $f"
            curl -sf -X POST \
              "https://api.supabase.com/v1/projects/uucxyuqxqjpbxecemdvf/database/query" \
              -H "Authorization: Bearer $PAT" \
              -H "Content-Type: application/json" \
              -d "{\"query\": $(jq -Rs . < "$f")}"
            echo "  ✓"
          done

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy ai-proxy function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy ai-proxy --project-ref uucxyuqxqjpbxecemdvf
